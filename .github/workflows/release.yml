name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # ── Create the GitHub Release first so parallel build jobs can upload to it ──
  create-release:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - name: Extract tag name
        id: meta
        run: echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.3.2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          generate_release_notes: true
          draft: false
          prerelease: false

  # ── Windows x64 build ──────────────────────────────────────────────────────
  build-windows:
    needs: create-release
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4.2.2

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build frontend (Vite)
        run: bun run vite build

      - name: Build app (Electrobun stable)
        run: bunx electrobun build --env=stable

      # Electrobun's rcedit calls are wrapped in silent try/catch and fail on CI.
      # We embed icons explicitly here after the build produces the binaries.
      # The launcher lives inside the app bundle — we embed the icon then repack
      # the tar.zst so both fresh installs and auto-updates get the correct icon.
      - name: Embed icons and repack tar.zst
        shell: pwsh
        run: |
          $rcedit  = "node_modules\rcedit\bin\rcedit-x64.exe"
          $icon    = "assets\icon.ico"
          $dir     = "build\stable-win-x64"
          $binDir  = "$dir\AutoDesk\bin"

          # Launcher may be named 'launcher' or 'launcher.exe' depending on OS/Bun behaviour
          $launcher = if (Test-Path "$binDir\launcher.exe") { "$binDir\launcher.exe" } else { "$binDir\launcher" }
          Write-Host "Embedding icon into launcher: $launcher"
          & $rcedit $launcher --set-icon $icon
          if ($LASTEXITCODE -ne 0) { Write-Error "rcedit failed on launcher"; exit 1 }

          # Install zstd for repacking
          choco install zstandard -y --no-progress | Out-Null

          # Repack tar.zst so the icon-embedded launcher is inside the archive
          Remove-Item "$dir\AutoDesk-Setup.tar.zst" -Force
          tar -cf - -C $dir AutoDesk | zstd -o "$dir\AutoDesk-Setup.tar.zst"
          Write-Host "Repacked AutoDesk-Setup.tar.zst with icon-embedded launcher"

          # Embed icon into the installer exe
          & $rcedit "$dir\AutoDesk-Setup.exe" --set-icon $icon
          if ($LASTEXITCODE -ne 0) { Write-Error "rcedit failed on AutoDesk-Setup.exe"; exit 1 }
          Write-Host "Icon embedded into AutoDesk-Setup.exe"

      # Bundle exe + archive into a single zip so users download one file.
      # Layout mirrors what electrobun's wrapWindowsInstallerInZip produces:
      #   AutoDesk-Setup.zip
      #   ├── AutoDesk-Setup.exe          ← user runs this
      #   └── .installer/
      #       ├── AutoDesk-Setup.metadata.json
      #       └── AutoDesk-Setup.tar.zst
      # Also rename files to the flat prefix-based names the updater expects:
      #   {baseUrl}/stable-win-x64-update.json          ← checked by Updater.checkForUpdate()
      #   {baseUrl}/stable-win-x64-AutoDesk.tar.zst     ← downloaded by Updater.downloadUpdate()
      - name: Prepare distribution files
        shell: pwsh
        run: |
          $dir = "build/stable-win-x64"

          # Build single-zip installer for user distribution
          $staging = "$dir/.installer-staging"
          New-Item -ItemType Directory -Force -Path "$staging/.installer" | Out-Null
          Copy-Item "$dir/AutoDesk-Setup.exe"           "$staging/AutoDesk-Setup.exe"
          Copy-Item "$dir/AutoDesk-Setup.metadata.json" "$staging/.installer/AutoDesk-Setup.metadata.json"
          Copy-Item "$dir/AutoDesk-Setup.tar.zst"       "$staging/.installer/AutoDesk-Setup.tar.zst"
          Compress-Archive -Path "$staging/*" -DestinationPath "$dir/AutoDesk-win-x64-Setup.zip" -Force
          Remove-Item -Recurse -Force "$staging"

          # update.json for Updater.checkForUpdate()
          Copy-Item "$dir/AutoDesk-Setup.metadata.json" "$dir/stable-win-x64-update.json"
          # Renamed tarball for Updater.downloadUpdate()
          Copy-Item "$dir/AutoDesk-Setup.tar.zst" "$dir/stable-win-x64-AutoDesk.tar.zst"

      - name: Upload Windows artifacts
        uses: softprops/action-gh-release@v2.3.2
        with:
          tag_name: ${{ needs.create-release.outputs.tag }}
          files: |
            build/stable-win-x64/AutoDesk-win-x64-Setup.zip
            build/stable-win-x64/stable-win-x64-update.json
            build/stable-win-x64/stable-win-x64-AutoDesk.tar.zst
          fail_on_unmatched_files: true

  # ── macOS arm64 build (Apple Silicon) ──────────────────────────────────────
  build-macos-arm64:
    needs: create-release
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4.2.2

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build frontend (Vite)
        run: bun run vite build

      - name: Build app (Electrobun stable)
        run: bunx electrobun build --env=stable

      - name: Prepare distribution files
        run: |
          set -e
          dir="build/stable-macos-arm64"

          # metadata.json is at Contents/Resources/metadata.json on macOS
          metadata="$dir/AutoDesk.app/Contents/Resources/metadata.json"
          echo "App metadata: $(cat $metadata)"

          # update.json — fetched by Updater.checkForUpdate()
          cp "$metadata" "$dir/stable-macos-arm64-update.json"

          # tar.zst — downloaded by Updater.downloadUpdate()
          tar -cf - -C "$dir" "AutoDesk.app" | zstd --stdout > "$dir/stable-macos-arm64-AutoDesk.app.tar.zst"

          # zip — for users to download and drag to /Applications
          cd "$dir" && zip -r "AutoDesk-mac-arm64.zip" "AutoDesk.app" && cd -

      - name: Upload macOS arm64 artifacts
        uses: softprops/action-gh-release@v2.3.2
        with:
          tag_name: ${{ needs.create-release.outputs.tag }}
          files: |
            build/stable-macos-arm64/AutoDesk-mac-arm64.zip
            build/stable-macos-arm64/stable-macos-arm64-update.json
            build/stable-macos-arm64/stable-macos-arm64-AutoDesk.app.tar.zst
          fail_on_unmatched_files: true

  # Intel (x64) macOS build intentionally omitted — macos-13 runners have very
  # long queue times on GitHub Actions. Apple Silicon covers the vast majority
  # of current Mac users. Re-add build-macos-x64 here if Intel support is needed.
